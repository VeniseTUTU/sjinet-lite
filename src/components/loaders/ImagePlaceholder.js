import React, {useState} from "react";
import '../../scss/imageplaceholder.scss';

const ImagePlaceholder = () => {

    return (
        <span className="imageplaceholder">
          
        </span>
      );
   
};

export default ImagePlaceholder;
